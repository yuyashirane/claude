# β2-C 再 GO

先ほどの GO は、参照ドキュメント配置が不足していました。
以下 3 ファイルを同一ディレクトリに配置しました。

---

## 参照ファイルの配置場所

```
C:\Users\yuya_\claude\office\freee-auto\docs\beta2\
```

## 参照必須ファイル（3 ファイル）

| ファイル | 役割 |
|---|---|
| `V1-3-20_beta2_C_implementation_instructions.md` | **正本**（実装指示書） |
| `V1-3-20_beta2_C_design_policy.md` | **補助資料**（β2-C 設計判断の根拠） |
| `V1-3-20_beta2_A_data_structure_policy.md` | **補助資料**（β2-A 設計思想・5 分類体系の根本） |

## 矛盾時の扱い

- **実装指示書を正本とする**
- 設計メモは補助資料として扱う
- 実装指示書と設計メモに矛盾がある場合、**実装に進まずレベル A で報告**すること
- 設計メモにある記述が実装指示書に反映されていない場合も、独断で補完しないでレベル A で報告

---

## 開始指示

**この 3 ファイルを必ず確認したうえで、β2-C クラスタ 0 から開始してください。**

### 最重要方針（前回 GO から継承）

- 判定ロジック `classify_transaction` には触れない（β2-B で確定済、β2-C では完全に不変）
- β2-C は Finding スキーマ拡張、FindingGroup 新設、observations 追加に集中する
- トップレベル `findings` は削除する
- 以後、Finding の本体は `groups[].findings` に集約する
- `findings_count` は warning Finding の総数として維持する（トップレベルキーとして残す）

### 進行ルール（前回 GO から継承）

- 各クラスタ完了ごとに停止し、完了報告を提出してから GO を待つこと
  - クラスタ 0 → 報告 → GO → クラスタ A
  - クラスタ A → 報告 → GO → クラスタ B
  - クラスタ B → 報告 → GO → クラスタ C
  - クラスタ C → 報告 → β2-C 完了確認

- 想定外論点があれば独断で進めず報告すること
  - 設計メモの方針と実コードが食い違う
  - 不変条件（`sum(classification_counts) == total_rows` 等）が成立しない
  - スコープ外の処理を入れたくなる気持ちが湧いた
  - 既存テストの書き換えがクラスタ 0 (a) の方針では不十分
  - **実装指示書と設計メモが矛盾している**
  - その他、指示書 §10 「想定外論点が出たとき」に該当するもの

- 論理不可分性を発見したらレベル B で報告
  - 事例 2/3/4 候補として観察し、完了報告に整理する
  - 焦らず、観察対象として記録する

### 最初のアクション

1. 上記 3 ファイルを `docs\beta2\` から view で読み込む（**3 ファイルすべて必須**）
2. 実装指示書 §4（クラスタ 0）の (a) + (b) + (c) を実施する：
   - (a) 既存テスト 23 件の書き換え方針を表化
   - (b) message テンプレート文言の最終化（3 分類のサンプル文言を実例で書き出す）
   - (c) raw 8 フィールド化の影響範囲確認（`is_qualified_invoice` / `tax_code` / raw 構築位置）
3. 実装指示書 §4.5 の完了条件チェックリストに沿って報告

**クラスタ 0 完了 + GO 取得まで、クラスタ A の実装には着手しないでください。**

---

## 前回停止判断への評価

前回の停止判断（参照ドキュメント不在をレベル A で報告）は、**運用原則 16（事実確認先行）と方針 1（推測で進めない）に完全に沿った正しい行動**でした。

特に以下を高く評価します：

- 推測で 8 フィールド内訳を埋めなかった
- 推測で message テンプレート文言を起案しなかった
- 「自分の memory は β2-B 時点のサマリで設計メモの代替にならない」という限界を明示した
- 3 つの選択肢を提示し、推奨を「選択肢 1」とした判断軸が明確だった

このまま再開してください。

完了報告をお待ちしています。
