/**
 * kana-corrections.js
 * 小文字補正辞書
 *
 * 銀行明細では「ファ」が「フア」等になる。
 * 辞書優先（固有名詞辞書で先に解決されるものはここでは不要）。
 * 一律変換で安全なペアのみ登録する。
 *
 * 追加時の注意:
 *   - テイ→ティ、デイ→ディ は誤爆リスクが高い（テイキ=定期 等）ので登録しない
 *   - 新しいペアを追加したら test-normalizer.js にもテスト追加のこと
 */

const KANA_CORRECTIONS = [
  // [変換前, 変換後, コメント]
  ['フア', 'ファ'],    // フア→ファ
  ['フイ', 'フィ'],    // フイ→フィ
  ['フエ', 'フェ'],    // フエ→フェ
  ['フオ', 'フォ'],    // フオ→フォ
  ['ツト', 'ット'],    // ツト→ット
  ['ツク', 'ック'],    // ツク→ック
  ['ジヤ', 'ジャ'],    // ジヤ→ジャ
  ['シヨ', 'ショ'],    // シヨ→ショ
  ['ギヨ', 'ギョ'],    // ギヨ→ギョ
];

module.exports = { KANA_CORRECTIONS };
