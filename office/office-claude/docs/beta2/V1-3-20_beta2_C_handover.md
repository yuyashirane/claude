# V1-3-20 β2-C 実装指示書作成セッション 引き継ぎ書

**作成日**: 2026-04-28
**目的**: 新セッションで β2-C 実装指示書作成を再開するための完全引き継ぎ
**前セッションの最終状態**: β2-C 設計フェーズ完結（6 論点確定 + 設計メモ作成完了）

---

## §1. このドキュメントの使い方

新セッションを開始したら、以下の順序で進めてください：

1. このドキュメント全体に目を通す
2. **§7 の「開始プロンプト」**をコピペして送信
3. 戦略 Claude が状況を把握したら、**β2-C 実装指示書の骨格レビューから開始**
4. 骨格 → レビュー → 本文 → GO の流れ（β2-B と同じパターン）

---

## §2. 現在の進行状況

### V1-3-20 全体ロードマップ

```
[完了] β2-A：データ構造方針確定（4 論点）
[完了] β2-B：5 分類体系 deals 単体実装（77 tests passed、実データ 2064 件分類）
[完了] β2-C 設計フェーズ：6 論点確定 + 設計メモ作成   ← 直前セッションで完了
[次] β2-C 実装指示書作成（このセッションでやること）  ← ここから
[待機] β2-C 実装（Claude Code）
[待機] β2-D：3 社観察 + manual_journals 判断 + 474381 fetch
[待機] β2-E：Excel 統合直前仕様
[β3] Excel exporter 本体 + T 番号妥当性チェック
```

### 直前セッションで完了したこと

V1-3-20 β2-C 設計フェーズの 6 論点を確定し、設計メモを作成しました：

| 論点 | 確定内容 |
|---|---|
| 1: Finding スキーマ | `classification: Classification \| None = None` 追加（Optional） |
| 2: raw 拡張 | tax_code + is_qualified_invoice の 2 つ追加（β1 6 → β2-C 8 フィールド） |
| 3: message | テンプレートベース + 共通構造、partner 空時「取引先不明」 |
| 4: FindingGroup | classification 単位、最小実装まで |
| 5: expected_* | groups に含めない（classification_counts のみ） |
| 6: observations | partner_unknown_breakdown のみ追加 |

成果物：`V1-3-20_beta2_C_design_policy.md`（636 行）

---

## §3. このセッションでやること

### 主タスク

**β2-C 実装指示書を作成して、Claude Code に渡せる状態にする**。

β2-B と同じ流れで進める：

1. **骨格作成**（戦略 Claude）
2. **骨格レビュー**（悠皓さん）
3. **本文作成**（戦略 Claude、マークダウンファイル）
4. **クラスタ 0 GO 発行**（or 必要に応じて事前調査）
5. **Claude Code 実装** → **完了報告 → GO → 次クラスタ**

### 想定される指示書の構造（参考）

β2-B 指示書（821 行）の構造を踏襲：

- §1: タスク全体像（最重要文）
- §2: 前提条件と参照先（β2-C 設計メモを引用）
- §3: クラスタ分割
- §4-§6: 各クラスタの詳細仕様
- §7: 完了報告フォーマット
- §8: 禁止事項
- §9: 段階的進行ルール
- §10: 既存ファイル変更点
- §11: 末尾強調
- §12: β2-D への申し送り
- §13: 開始指示

---

## §4. クラスタ分割の予備案（骨格議論用）

直前セッションでの議論から、以下のクラスタ分割が想定される：

### 案 1：β2-B 同様の 3 クラスタ + クラスタ 0

| クラスタ | 内容 | 主な変更ファイル |
|---|---|---|
| **0**(前提) | 既存テスト 23 件への影響範囲確認 | （調査のみ） |
| **A** | Finding スキーマ拡張 + raw 8 フィールド化 + message テンプレート実装 | `schema.py`, `checker.py`, `run.py` |
| **B** | FindingGroup 設計 + 実装 + main() 結線 | `schema.py`, `run.py` |
| **C** | observations 実装 + テスト書き換え + 実データ検証 | `run.py`, `tests/` |

### 案 2：2 クラスタ + クラスタ 0（β2-B より凝縮）

| クラスタ | 内容 |
|---|---|
| **0** | 既存テスト影響調査 |
| **A** | Finding 拡張 + FindingGroup（schema 系を一括） |
| **B** | observations + テスト書き換え + 実データ検証 |

### 推奨：案 1（3 クラスタ）

理由：
- β2-B でクラスタ A の踏み込み（main() 改修）が論理不可分で発生 → β2-C も同様の懸念あり
- Finding 拡張 と FindingGroup 新設は別軸 → 分けた方が観察しやすい
- 論理不可分性事例の継続観察（K5）に有効

---

## §5. 重要な観察軸（β2-C 実装で継続）

直前セッション最後に確定した観察軸 3 つ。新セッションでも継続する：

| 観察軸 | 内容 |
|---|---|
| **論理不可分 1** | Finding schema 変更 ↔ checker.py（`to_findings` の引数追加） |
| **論理不可分 2** | FindingGroup 新設 ↔ run.py 出力（main() 改修） |
| **論理不可分 3** | 観察用出力 ↔ CLI JSON 構造（TestExitZeroEndToEnd 等の影響） |

これらが β2-C 実装で発生するか観察し、**事例 2（β2-B クラスタ A での事例 1 の再発）**として記録する。事例が 2 つ以上揃えば、運用原則 18「クラスタ分割時の論理的不可分性チェック」として原則化検討する（β2-D 以降）。

---

## §6. 必読ドキュメント（新セッションで参照すべきもの）

新セッション開始時に、以下のドキュメントが参照対象になります。これらは **outputs フォルダ** にすべて存在しています：

### β2-C 関連（最重要）

- **`V1-3-20_beta2_C_design_policy.md`**（636 行）
  - 6 論点の確定内容、JSON 出力構造、テスト戦略すべて記載
  - **β2-C 実装指示書はこれを引用元として作成する**

### β2-A / β2-B 関連（参考）

- `V1-3-20_beta2_roadmap.md`（ロードマップ全体）
- `V1-3-20_beta2_A_data_structure_policy.md`（β2-A 確定事項）
- `V1-3-20_beta2_B_implementation_instructions.md`（β2-B 指示書、構造の参考）
- `V1-3-20_beta2_B_completion_notice.md`（β2-B 完結状態）

### β1 関連（背景）

- `V1-3-20_beta1_completion_notice.md`（β1 完結状態）

### memory（自動参照）

新セッションでは memory が自動的に読み込まれます。特に重要なもの：

- #6: V1-3-10 期間設計（用語ギャップ修正済み）
- #9: 実装状況（V1-3-10 + V1-3-20 β2-B が実稼働中）
- #28: 運用原則 17（マークダウンファイル化）
- #29: V1-3-20 β1 完結記録
- #30: V1-3-20 β2-B 完結記録（**β2-C 設計メモへの参照を含む**）

---

## §7. 開始プロンプト（新セッションへコピペ用）

以下のテキストを新セッションの最初のメッセージとしてコピペしてください：

```
V1-3-20 β2-C 実装指示書の作成をお願いします。

## 状況

直前セッションで V1-3-20 β2-C 設計フェーズが完結しました。
6 論点すべて確定済みで、β2-C 設計メモ（V1-3-20_beta2_C_design_policy.md、636 行）を作成済みです。

このセッションでやることは、その設計メモを引用元として、
**β2-C 実装指示書を作成し、Claude Code に渡せる状態にする**ことです。

## 進め方

β2-B と同じパターンで進めてください：

1. 骨格作成 → レビュー → 本文 → GO の流れ
2. まず骨格を提示してください（本文には進まず）

## 確定済みの設計判断（要約）

| 論点 | 確定 |
|---|---|
| 1: Finding スキーマ | classification: Optional 追加 |
| 2: raw 拡張 | tax_code + is_qualified_invoice 追加（8 フィールド化） |
| 3: message | テンプレートベース、partner 空時「取引先不明」 |
| 4: FindingGroup | classification 単位、最小実装まで |
| 5: expected_* | groups に含めない |
| 6: observations | partner_unknown_breakdown のみ |

詳細は V1-3-20_beta2_C_design_policy.md を参照してください。

## 重要な観察軸（K5 継続）

論理不可分性の観察軸 3 つを継続します：
- Finding schema 変更 ↔ checker.py
- FindingGroup 新設 ↔ run.py 出力
- 観察用出力 ↔ CLI JSON 構造

事例 1（β2-B クラスタ A）に続いて、β2-C で事例 2 が出るかを観察対象として明記してください。

## クラスタ分割の予備案

私の推奨は β2-B 同様の 3 クラスタ + クラスタ 0 ですが、骨格レビュー時に確認させてください：

- クラスタ 0: 既存テスト 23 件への影響範囲確認
- クラスタ A: Finding 拡張 + raw 拡張 + message テンプレート
- クラスタ B: FindingGroup 実装 + main() 結線
- クラスタ C: observations + テスト書き換え + 実データ検証

## 必読ドキュメント

このセッションで参照すべきドキュメントは outputs フォルダにあります：

最重要:
- V1-3-20_beta2_C_design_policy.md（β2-C 設計メモ、引用元）

参考:
- V1-3-20_beta2_B_implementation_instructions.md（β2-B 指示書、構造の参考）
- V1-3-20_beta2_B_completion_notice.md（β2-B 完結状態）
- V1-3-20_beta2_A_data_structure_policy.md（β2-A 確定事項）

## 確認

最初のメッセージとして、以下を実施してください：

1. β2-C 設計メモ（V1-3-20_beta2_C_design_policy.md）を view で読み込み内容を確認
2. β2-B 指示書（V1-3-20_beta2_B_implementation_instructions.md）の構造を view で確認（参考用）
3. β2-C 実装指示書の骨格を提示
4. クラスタ分割の予備案について確認質問
5. 私のレビューを待つ

骨格レビューが終わったら本文作成に進みます。

開始してください。
```

---

## §8. 引き継ぎチェックリスト（新セッション開始時）

新セッションで戦略 Claude が確認すべきこと：

- [ ] memory #29（β1 完結）と #30（β2-B 完結）を理解している
- [ ] β2-C 設計メモ（V1-3-20_beta2_C_design_policy.md）の 6 論点確定内容を把握している
- [ ] β2-B 指示書の構造（13 章構成、§1〜§13）を参考できる状態
- [ ] 観察軸 3 つを念頭に置いている
- [ ] クラスタ分割の予備案（3 クラスタ案）を把握している
- [ ] 運用原則 17（マークダウンファイル化）を順守する
- [ ] 「骨格 → レビュー → 本文 → GO」の流れを順守する

---

## §9. 補足：論理不可分性の事例ラベル付き保存

直前セッションで悠皓さんから「memory には登録せず、ラベル付き一時保存」と指示されたメモ：

```
[β2-B / β2-C 設計メモ候補]
- 事例 1（記録済み）: クラスタ A の論理不可分性（β2-B）
  → find_candidates 削除と main() 改修が論理的に不可分だった件

- 観察軸 1（β2-C で観察予定）: Finding schema 変更 ↔ checker.py
- 観察軸 2（β2-C で観察予定）: FindingGroup 新設 ↔ run.py 出力
- 観察軸 3（β2-C で観察予定）: 観察用出力 ↔ CLI JSON 構造

→ β2-C 完結時に事例 1 + (β2-C で発生した事例) で原則化検討
→ 事例 2 つ以上揃えば運用原則 18 化候補
```

新セッションでも、この観察視点を維持してください。

---

## §10. このセッションで触れない方がよいこと

β2-C 実装指示書作成に集中するため、以下は触らない：

- ❌ V1-3-10 統合判断（β2 全体の持ち越し論点、β2-D 以降）
- ❌ Excel exporter 本体（β2-E / β3）
- ❌ T 番号妥当性チェック（β3）
- ❌ manual_journals 対応（β2-D）
- ❌ Phase C-1 fetch 層総点検（別タスク）
- ❌ 2-J 失敗パターン集執筆（別タスク）

---

## §11. 完結後の次ステップ

β2-C 実装指示書作成完了後の流れ：

1. クラスタ 0 GO（必要なら事前調査）
2. クラスタ 0 完了報告 → 戦略 Claude レビュー → クラスタ A GO
3. クラスタ A 完了報告 → レビュー → クラスタ B GO
4. クラスタ B 完了報告 → レビュー → クラスタ C GO
5. クラスタ C 完了報告 → レビュー → β2-C 完結
6. β2-D 着手判断（manual_journals + 3 社観察 + 474381 fetch）

→ β2-B と同じセッション数（実装指示書 + 1〜2 セッション）が想定される。

---

## §12. 一言

このセッションは「設計判断は完了済み、それを実装可能な指示書に落とす」フェーズです。
判断の余地は最小限で、整合性のある指示書を**精度よく書ける**状態が整っています。

β2-C 設計メモが引用元として完璧に機能するので、書きやすいはずです。
